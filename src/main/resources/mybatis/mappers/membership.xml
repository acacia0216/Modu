<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="membership">

<update id="insertMembership" parameterType="GroupVo">
		<![CDATA[
		update group_tbl
		set memberFeeAmount = #{memberFeeAmount},
        memberFeeDate = #{memberFeeDate},    
        memberFeeinterval = #{memberFeeinterval},    
        groupBank = #{groupBank},    
        groupAccount = #{groupAccount},    
        groupAccountHolder = #{groupAccountHolder}    
        where groupNo =#{groupNo}      
		]]>
	</update>
	
	
<insert id="insertPayment" parameterType="MembershipVo">

       <![CDATA[
		    insert into memberfee_tbl(memberfeeNo,user_groupNo,paymentFee,paymentDate)
		    VALUES(seq_memberfeeNo.NEXTVAL
		     ,#{user_groupNo}
             ,#{paymentFee}
             ,#{paymentDate})
        ]]>

</insert>

<select id="selectMembership"  parameterType="int"  resultType="GroupVo">
	 <![CDATA[
             Select groupNo,memberFeeAmount,memberFeeDate,groupBank,groupAccount,groupAccountHolder,memberFeeinterval
               from group_tbl
               where groupNo=#{groupNo}
             ]]>
	</select>
	
<select id="selectMembershipList"  parameterType="int"  resultType="MembershipVo">
	 <![CDATA[
            select memberfeeNo,user_groupNo,paymentFee,paymentDate,userName
            from USER_GROUP_TBL UG ,USER_TBL U , MEMBERFEE_TBL M
            where UG.USERNO = U.USERNO 
            and UG.USER_GROUPNO = M.USER_GROUPNO
            and UG.GROUPNO=#{groupNo}
             ]]>
	</select>

	
	
</mapper>